const INITIAL_STATE = {
  user: { name: 'Popo', balance: 100 }
}
export function userReducer(state = INITIAL_STATE, action = {}) {
  switch (action.type) {
    case '':

      break;

    default:
      return state
  }
}